This update pertains to the Files > Directory Browser & Files lists

- Need to implement the functionality to refresh the directory browser manually

- Possible to implement an "Up Directory" functionality into the directory browser? (both source and local files)

- Need to be able to create directories/sub-folders from the directory browser (no document control action necessary?)

- For the local files tab, users should be able to create/rename/delete directories.

- For the local files tab, users should be able to rename/move/delete files (no document control action necessary?)

- For the local files tab, users should be able to add files to favorites/global favorites similar to how this action works with Source > Files as well.

- For the local files tab, users should be able to create reference copies of files similar to how this action works with Source > Files as well.