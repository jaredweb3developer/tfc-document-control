Aftern running the updated UI I have a couple of small changes that I would like to make that differ from the original documentation:

- If space is not a constraint, in the Projects section I would like to replace the `Favorites` tab with the two respective tabs it currently encompasses rather than nesting them. Those tabs being `Project Favorites` and `Global Favorites`

- Similarly I would like to do the same with the `Files` tab and instead have tabs for `Checked Out` and `Reference Files` instead of nesting them as well.

- I would also like the `Search Files` input box in the files section to be a bit wider (approximately 2x width) than what it is currently.